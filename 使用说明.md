# Advanblack 本地商品表现

双击「启动看板.cmd」，或在服务已经运行时打开 http://127.0.0.1:4318/ 。网页只监听本机，不发布到公网。

当前数据覆盖 2026-09-04 至 2026-09-23。可在这段日期内任意筛选、搜索、排序及导出表格。覆盖范围外会提示补充数据，不会把未同步的数据显示成零。

## 统计口径

- 主表：GA4 `advanblack - GA4`（378788930）商品 Title。
- 订单数：购买商品记录按交易编号去重，不用购买件数替代订单数。总订单卡片跨商品再次去重。
- Sessions：商品维度每日会话数汇总，不可跨商品相加当作全站会话数。
- 收入：GA4 全渠道 itemRevenue，USD，扣除商品退款，不含运费、税费。
- Meta：03、04 两个账户。凡报表返回 Product ID 的消耗都纳入，包括普通广告中的产品明细；余额保留待归因。Product ID 字符串按第一个逗号拆出原始 ID 和完整 Title。
- Google：主表合并 PMax 和 Shopping 商品消耗为 Google USD，按用户指定固定汇率 **1 USD = 6.8 CNY** 换算。其他广告系列暂不分配。
- GA4 以洛杉矶时间统计，广告以北京时间统计，日期范围按各自平台日期筛选。

## 商品匹配

先按唯一的规范化 Title 匹配，再按经过检查的 SKU 系列及产品类型归并颜色、五金变体。型号归并是规则推断，不是平台提供的转化归因。King、Chopped、Razor 及其内衬等配件分开处理。点击商品 Title 可以查看原始广告商品、Product ID、消耗和规则依据。

无法确定的产品留在「待归因产品」。你可以选择正确的 GA4 Title 并确认映射，映射仅保存在当前浏览器。需要撤销时使用「数据与口径」中的撤销按钮。

## 后续更新

目前是本地数据查询版。「读取最新本地数据」只重读本地报表，不调用平台接口。查询更早或更新的日期时，可在原任务中要求同步指定时间段。独立于当前任务的自动联网同步尚未配置，需要额外的本地 API 或 MCP 连接授权；页面不会假装已经联网更新。

原始报表位于 data 文件夹，网页只开放汇总数据及静态页面。构建后的网页使用交易编号的 SHA-256 值去重。金额按原始精度计算，显示保留两位小数。
